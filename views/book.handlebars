<div class="book-page">
    <div class="container">
        <h1>{{book.title}}</h1>
        <p>Este livro tem {{book.nm_paginas}} páginas</p>
        <div class="actions">
            <a href="/book/edit/{{book.id}}">editar</a>
            <a href="/books/deletebook/{{book.id}}">excluir</a>
        </div>
    </div>
</div>